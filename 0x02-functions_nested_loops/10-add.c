#include "main.h"
/**
 * add - Adds two integers and returns the result
 * @i: to be added to j
 * @j: to be added to i
 * Return: sum of i and j
 */
int add(int i, int j)
{
	int result;

	result = i + j;
	return (result);
}
